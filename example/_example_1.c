#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "hindi.h"

// फ़ाइल स्तर पर संरचना की परिभाषा
अनुक्रम छात्र
{
    अक्षर नाम[50];
    पूरा उम्र;
    दायमिक अंक;
};

// फ़ंक्शन घोषणाएँ
पूरा प्रिंट_छात्र(अनुक्रम छात्र *s);
दायमिक औसत_अंक(अनुक्रम छात्र *s1, अनुक्रम छात्र *s2, अनुक्रम छात्र *s3);

पूरा मुख्य()
{
    // चर घोषित करना
    अनुक्रम छात्र छात्र1, छात्र2, छात्र3;
    दायमिक औसत;

    // छात्रों को प्रारंभ करना
    छात्र1.उम्र = 20;
    छात्र2.उम्र = 22;
    छात्र3.उम्र = 19;

    // पॉइंटर
    अनुक्रम छात्र *ptr1 = &छात्र1;
    अनुक्रम छात्र *ptr2 = &छात्र2;
    अनुक्रम छात्र *ptr3 = &छात्र3;

    // नाम निर्दिष्ट करना (डमी डेटा)
    strcpy(ptr1->नाम, "Ravi");
    strcpy(ptr2->नाम, "Sita");
    strcpy(ptr3->नाम, "Amit");

    // अंक निर्दिष्ट करना
    ptr1->अंक = 85.5;
    ptr2->अंक = 90.2;
    ptr3->अंक = 78.8;

    // छात्र जानकारी प्रिंट करना
    प्रिंट_छात्र(ptr1);
    प्रिंट_छात्र(ptr2);
    प्रिंट_छात्र(ptr3);

    // औसत निकालना
    औसत = औसत_अंक(ptr1, ptr2, ptr3);
    लिखो("Average score: %f\n", औसत);

    लौटाओ 0;
}

// फ़ंक्शन परिभाषाएँ
पूरा प्रिंट_छात्र(अनुक्रम छात्र *s)
{
    लिखो("Name: %s, Age: %d, Score: %.2f\n", s->नाम, s->उम्र, s->अंक);
    लौटाओ 0;
}

दायमिक औसत_अंक(अनुक्रम छात्र *s1, अनुक्रम छात्र *s2, अनुक्रम छात्र *s3)
{
    // कुल अंक निकालना
    दायमिक कुल = s1->अंक + s2->अंक + s3->अंक;
    लौटाओ कुल / 3.0;
}
